{
  "name": "campus-feedback-system",
  "version": "1.0.0",
  "description": "Campus Honest and Transparent Feedback System (Thapar University)",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"AUTH,FEEDBACK,GATEWAY,FRONTEND\" -c \"blue,green,magenta,cyan\" \"npm run dev --prefix auth-service\" \"npm run dev --prefix feedback-microservice\" \"npm run dev --prefix gateway\" \"npm run dev --prefix frontend\"",
    "start": "concurrently -n \"AUTH,FEEDBACK,GATEWAY,FRONTEND\" -c \"blue,green,magenta,cyan\" \"npm start --prefix auth-service\" \"npm start --prefix feedback-microservice\" \"npm start --prefix gateway\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install && npm install --prefix auth-service && npm install --prefix feedback-microservice && npm install --prefix gateway && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
