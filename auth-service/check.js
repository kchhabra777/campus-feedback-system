import dotenv from 'dotenv'; dotenv.config({ path: '../.env' }); import prisma from './src/lib/prisma.js'; async function main() { const t = await prisma.teacherProfile.findFirst({ where: { fullName: { contains: 'Shivani Sharma', mode: 'insensitive' } }, include: { suggestions: true } }); console.log(JSON.stringify(t, null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
